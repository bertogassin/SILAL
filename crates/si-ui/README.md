# si-ui

View-models and tokens live in `design/tokens.json` and the web client (`src/components/si`). A Dioxus/Slint crate will consume `si-core` here when native UI lands. Do not mix three UI frameworks.
